name: IMM OS Backend CI
on: [push, pull_request]
jobs:
  lint:
      runs-on: ubuntu-latest
          steps:
                - run: echo "lint"
                  test:
                      runs-on: ubuntu-latest
                          steps:
                                - run: echo "test"
                                  build:
                                      runs-on: ubuntu-latest
                                          steps:
                                                - run: echo "build"
